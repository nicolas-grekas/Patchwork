- mode verrouillé pour QSelect + value
- cache des données USER et AUTH dans SESSION, lever le problème avec les références
- iaForm : ajouter 'default' et 'disabled' à $param
- intégrer une gestion de cache pour les loop ? un mécanisme de watch ?
- barre d'upload : annuler si la taille dépasse la limite définie ? => oui si on règle le pb de la taille supplémentaire ajoutée par les éléments autre que des fichiers, et le surplus lié à l'encodage
- autorefresh du cache lorsque le CIApID change : deux techniques : le navigateur détecte et fait la requete de mise à jour, ou bien le serveur track les chgt