    * cache des données USER et AUTH dans SESSION, lever le problème avec les références
    * définir qqch de similaire à __AGENT__ pour les scripts PHP ?.
    * Code de debug/test possible dans les JS et les PHP, executé SSI DEBUG
    * autoscroll des formulaires : essayer de voir si on ne peut pas le faire avant "onload", au moment où la page a atteind une hauteur suffisante ?
    * Tester avec Safari
    * CIA::execAgent(...)
    * {r$...} indispensable pour un compilateur de templates base agents
    * Donner au designer la possibilité d'importer les donner d'un agent via <!-- IMPORT $agent --> ?
    * QSelect locké : <TAB> avant première recherche => search(value) => value = 1er résultat
    * Dossier tmp devrait varier avec CIA_ROOT
    * Faire une watchTable unique, ajouter une directive de configuration $watchPrefix pour les projets
    * MAILER :
          o composer le mails en html via des agents,
          o le donner à w3m pour créer une version texte,
          o ajouter à un pool d'envoi et faire requête de vidange de pool
          o faire un gestion des bounces et des reply + stockage des mails envoyés via une mailbox standard 
    * réussir à fonctionner en mode CGI => ur type "annuaire.php?fr/atlas?q=espci" supprimer la notion de <base href> et introduire un raccourcis pour {g$__ROOT__}